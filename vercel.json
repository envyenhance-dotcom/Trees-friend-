{
  "buildCommand": "pnpm install --frozen-lockfile && cd artifacts/tree-marketplace && pnpm build",
  "outputDirectory": "artifacts/tree-marketplace/dist",
  "installCommand": "pnpm install --frozen-lockfile"
}
